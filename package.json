{
  "name": "fix-endnotes",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "build": "esbuild src/main.js --bundle --external:obsidian --outfile=dist/main.js --platform=node --format=cjs --allow-overwrite",
    "dev": "esbuild src/main.js --bundle --external:obsidian --outfile=dist/main.js --platform=node --format=cjs --allow-overwrite --watch",
    "test": "node --test"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}
